apiVersion: v1
kind: ServiceAccount
metadata:
  name: sqs-processor
  namespace: sqs-processor
  # eks.amazonaws.com/role-arn is set by Terraform (platform_bootstrap) after apply.
  # It is intentionally absent here so ArgoCD does not override the live annotation.
