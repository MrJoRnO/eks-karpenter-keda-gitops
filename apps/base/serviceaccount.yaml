apiVersion: v1
kind: ServiceAccount
metadata:
  name: sqs-processor
  namespace: sqs-processor
  annotations:
    # Injected by Terraform (platform_bootstrap) after apply.
    eks.amazonaws.com/role-arn: ""
